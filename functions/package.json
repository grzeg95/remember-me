{
  "name": "functions",
  "scripts": {
    "lint": "tslint --project tsconfig.json",
    "build": "echo removing old generated lib && (if exist lib rmdir /s /q lib) && echo removed && echo tsc: start && tsc && echo tsc: done && python minify.py",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy:dev": "firebase use dev && firebase deploy --only functions:userOnCreate",
    "deploy:prod": "firebase use default && firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "16"
  },
  "main": "lib/functions/src/index.js",
  "dependencies": {
    "@google-cloud/kms": "~2.11.0",
    "fast-crc32c": "~2.0.0",
    "firebase-admin": "~10.0.2",
    "firebase-functions": "~3.18.0"
  },
  "devDependencies": {
    "tslint": "~6.1.3",
    "typescript": "~4.1.0"
  },
  "private": true
}
